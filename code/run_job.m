function run_job(dataset,alg,b)
disp([dataset '-' alg ' -b=' num2str(b)]);

%% load dataset
data = loadDataset(dataset);

%% initialize results parameters

for run = 1:nRuns
    %% shuffle examples
    %% run shampo
    %% test results
    
    
end
%% save results to file

end